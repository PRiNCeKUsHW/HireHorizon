# Dependencies for running on Termux (or any SQLite-only setup).
# Same as requirements.txt without the Postgres driver, which has no Android
# wheel and is not needed when DATABASE_URL is unset.
Django>=5.2,<6.0
dj-database-url>=2.2
whitenoise>=6.7
python-dotenv>=1.0
gunicorn>=22.0
