# HireHorizon — Django, text-only. No SMTP, no image handling (no Pillow).
Django>=5.2,<6.0
dj-database-url>=2.2
whitenoise>=6.7
python-dotenv>=1.0
gunicorn>=22.0

# Postgres driver, for deployments that set DATABASE_URL. Do not install this
# on Termux: there is no Android wheel, and SQLite is used there instead.
# Termux should install requirements-termux.txt, which omits this line.
# (An environment marker cannot express that — Termux's Python reports
# sys.platform == "linux", the same as any other Linux host.)
psycopg[binary]>=3.2
